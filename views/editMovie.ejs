<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1a1a1a;
            color: #fff;
            font-family: 'Arial', sans-serif;
            overflow-x: hidden;
        }

        /* Header styling */
        header {
            padding: 20px;
        }

        header h2 {
            color: #fff;
            margin: 0;
        }

        header ul {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        header ul li {
            display: inline;
            margin-left: 20px;
        }

        header ul li a {
            color: #fff;
            text-decoration: none;
        }

        header ul li a:hover {
            color: #ddd;
        }

        h1 {
            color: #6f42c1;
            text-align: center;
            margin-top: 50px;
            font-size: 2.5rem;
        }

        .form-container {
            max-width: 600px;
            margin: 50px auto;
            background-color: #222;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.6);
        }

        label {
            color: #6f42c1;
            font-weight: bold;
        }

        .form-control {
            background-color: #333;
            color: #fff;
            border: 1px solid #6f42c1;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .form-control:focus {
            background-color: #333;
            color: #fff;
            border-color: #6f42c1;
            box-shadow: none;
        }

        .form-control::placeholder {
            color: #bbb;
        }

        .form-control-file {
            background-color: #333;
            color: #bbb;
            border: 1px solid #6f42c1;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 8px;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #6f42c1;
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 1.2rem;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #5a33a2;
        }

        button:focus {
            box-shadow: none;
        }

        .img-thumbnail {
            max-width: 100%;
            margin-bottom: 15px;
            border: 2px solid #6f42c1;
        }
    </style>
</head>

<body>
    <header class="d-flex justify-content-between align-items-center"></header>
    <h2 class="ps-4">Movies</h2>
    <ul class="d-flex">
        <li><a href="/">Home</a></li>
        <li><a href="/createMovie">Create</a></li>
    </ul>
    </header>

    <h1>Edit Movie</h1>

    <!-- Edit Movie Form -->
    <div class="form-container">
        <form method="post" action="/update/<%= data._id %>" enctype="multipart/form-data">
            <!-- Hidden field for movie ID -->
            <input type="hidden" name="id" value="<%= data._id %>" />

            <!-- Movie title -->
            <div class="mb-3">
                <label for="title" class="form-label">Movie Title</label>
                <input type="text" name="title" id="title" class="form-control" value="<%= data.title %>" required>
            </div>

            <!-- Movie year -->
            <div class="mb-3">
                <label for="year" class="form-label">Release Year</label>
                <input type="number" name="year" id="year" class="form-control" value="<%= data.year %>" required>
            </div>

            <!-- Movie description -->
            <div class="mb-3">
                <label for="discription" class="form-label">Movie Description</label>
                <textarea name="discription" id="discription" class="form-control" rows="4"
                    required><%= data.discription %></textarea>
            </div>

            <!-- Current movie poster -->
            <div class="mb-3">
                <label for="poster" class="form-label">Current Poster</label>
                <% if (data.path) { %>
                    <img src="../<%= data.path %>" alt="<%= data.title %>" class="img-thumbnail">
                    <p>Current file: <a href="../<%= data.path %>" class="text-decoration-underline text-light">View
                            File</a></p>
                    <% } else { %>
                        <p>No poster uploaded.</p>
                        <% } %>
            </div>

            <!-- File upload for new poster -->
            <div class="mb-3">
                <label for="poster" class="form-label">Upload New Poster</label>
                <input type="file" name="poster" id="poster" class="form-control-file">
            </div>

            <!-- Submit button -->
            <button type="submit">Save Changes</button>
        </form>
    </div>


    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>