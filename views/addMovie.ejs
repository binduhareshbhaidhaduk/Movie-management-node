<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Movie</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1a1a1a;
            color: #fff;
            font-family: 'Arial', sans-serif;
            overflow-x: hidden;
        }


        /* Header styling */
        header {
            padding: 20px;
        }

        header h2 {
            color: #fff;
            margin: 0;
        }

        header ul {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        header ul li {
            display: inline;
            margin-left: 20px;
        }

        header ul li a {
            color: #fff;
            text-decoration: none;
        }

        header ul li a:hover {
            color: #ddd;
        }

        h1 {
            color: #6f42c1;
            text-align: center;
            margin-top: 50px;
            font-size: 2.5rem;
        }

        .form-container {
            max-width: 600px;
            margin: 50px auto;
            background-color: #222;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.6);
        }

        label {
            color: #6f42c1;
            font-weight: bold;
        }

        .form-control {
            background-color: #333;
            color: #fff;
            border: 1px solid #6f42c1;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .form-control:focus {
            background-color: #333;
            color: #fff;
            border-color: #6f42c1;
            box-shadow: none;
        }

        .form-control::placeholder {
            color: #bbb;
        }

        .form-control-file {
            background-color: #333;
            color: #bbb;
            border: 1px solid #6f42c1;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 8px;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #6f42c1;
            border: none;
            border-radius: 8px;
            color: #fff;
            font-size: 1.2rem;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #5a33a2;
        }

        button:focus {
            box-shadow: none;
        }
    </style>
</head>

<body>
    <header class="d-flex justify-content-between align-items-center">
        <h2 class="ps-4">Movies</h2>
        <ul class="d-flex">
            <li><a href="/">Home</a></li>
            <li><a href="/createMovie">Create</a></li>
        </ul>
    </header>

    <h1>Add Movie</h1>

    <div class="form-container">
        <form method="post" action="/addMovie" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="title" class="form-label">Movie Title</label>
                <input type="text" name="title" id="title" class="form-control" placeholder="Enter movie title"
                    required>
            </div>
            <div class="mb-3">
                <label for="year" class="form-label">Release Year</label>
                <input type="number" name="year" id="year" class="form-control" placeholder="Enter movie year" required>
            </div>
            <div class="mb-3">
                <label for="discription" class="form-label">Movie Discription</label>
                <textarea name="discription" id="discription" class="form-control" rows="4"
                    placeholder="Enter movie description" required></textarea>
            </div>
            <div class="mb-3">
                <label for="poster" class="form-label">Movie Poster</label>
                <input type="file" name="poster" id="poster" class="form-control-file" required>
            </div>
            <button type="submit">Add Movie +</button>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>