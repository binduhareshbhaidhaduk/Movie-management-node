<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Details</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #1a1a1a;
            color: #fff;
            font-family: 'Arial', sans-serif;
            overflow-x: hidden;
        }

        /* Header styling */
        header {
            padding: 20px;
        }

        header h2 {
            color: #fff;
            margin: 0;
        }

        header ul {
            list-style: none;
            margin: 0;
            padding: 0;
        }

        header ul li {
            display: inline;
            margin-left: 20px;
        }

        header ul li a {
            color: #fff;
            text-decoration: none;
        }

        header ul li a:hover {
            color: #ddd;
        }
        .container {
            margin-top: 50px;
            max-width: 800px;
            background-color: #222;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
        }
        .poster {
            display: block;
            margin: 0 auto 20px;
            border-radius: 10px;
            width: 100%;
            max-width: 400px;
        }
        .movie-title {
            font-size: 2em;
            color: #6f42c1;
            text-align: center;
            margin-bottom: 10px;
        }
        .movie-year {
            font-size: 1.2em;
            text-align: center;
            color: #bbb;
            margin-bottom: 20px;
        }
        .movie-description {
            font-size: 1.1em;
            text-align: justify;
            color: #ddd;
            line-height: 1.6;
            margin-bottom: 30px;
        }
        .action-buttons {
            text-align: center;
        }
        .action-buttons a {
            color: #fff;
            text-decoration: none;
            margin: 0 10px;
            padding: 10px 20px;
            background-color: #6f42c1;
            border-radius: 5px;
        }
        .action-buttons a:hover {
            background-color: #5a33a2;
        }
    </style>
</head>

<body>
    <header class="d-flex justify-content-between align-items-center">
        <h2 class="ps-4">Movies</h2>
        <ul class="d-flex">
            <li><a href="/">Home</a></li>
            <li><a href="/createMovie">Create</a></li>
        </ul>
    </header>
    <div class="container text-center">
        <img src="../<%=data.path %>" alt="<%= data.title %>" class="poster">
        <div class="movie-title"><%= data.title %></div>
        <div class="movie-year">Year: <%= data.year %></div>
        <p class="movie-description"><%= data.discription %></p>
        <div class="action-buttons">
            <a href="/singleMovie/<%= data._id %>">Edit</a>
            <a href="/deleteMovie/<%= data._id %>">Delete</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
